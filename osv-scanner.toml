# OSV-Scanner allowlist. Each entry must document why the advisory is not
# exploitable in this codebase. Keep this list minimal and revisit on review.
#
# Empty on purpose: every advisory this tree carries currently has a reachable
# fix. The brace-expansion entry that lived here is gone because upstream
# backported the DoS fix to the 1.x line (1.1.17, then 1.1.18 for the follow-up
# bypass), so the "no fixed release on 1.x" that justified it is no longer true
# and minimatch@3's own `^1.1.7` range takes the fix with no override.
