diff --git a/dist/Mcp.js b/dist/Mcp.js
index d916f16d0d3789a809d7a90d88ed6de7844708fd..4125dbc6fc1cf71beaced21f6015b532f4300a1d 100644
--- a/dist/Mcp.js
+++ b/dist/Mcp.js
@@ -40,7 +40,13 @@ function importStdioModule() {
         .then((module) => ({ module }))
         .catch((error) => ({ error }));
 }
-const importModule = (specifier) => import(specifier);
+const importModule = (specifier) => {
+    if (specifier === '@modelcontextprotocol/server/stdio')
+        return import('@modelcontextprotocol/server/stdio');
+    if (specifier === '@modelcontextprotocol/server')
+        return import('@modelcontextprotocol/server');
+    return import(specifier);
+};
 /** @internal Executes a tool call and returns a CallToolResult. */
 export async function callTool(tool, params, options = {}) {
     const allMiddleware = [
